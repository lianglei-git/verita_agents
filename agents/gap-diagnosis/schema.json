{
  "input": {
    "profile": { "type": "object", "description": "PlanningProfile（推荐）" },
    "handoff": { "type": "object", "description": "user-profile 放行 handoff（自动桥接）" },
    "planning_profile": { "type": "object", "description": "PlanningProfile 别名" },
    "force": { "type": "boolean", "description": "忽略 readiness 强制诊断" },
    "heuristic_only": { "type": "boolean", "description": "跳过 LLM，仅用规则" }
  },
  "output": {
    "output": { "type": "string", "description": "差距摘要" },
    "gap_diagnosis": { "type": "object", "description": "GapDiagnosis 契约" },
    "profile": { "type": "object", "description": "规范化后的 PlanningProfile" },
    "validation_issues": { "type": "array", "description": "契约/安全校验问题" },
    "blocked": { "type": "boolean", "description": "是否因 readiness 阻断" }
  }
}
