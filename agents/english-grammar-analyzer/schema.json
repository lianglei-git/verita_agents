{
  "input": {
    "type": "string",
    "description": "English sentence to analyze",
    "examples": [
      "The quick brown fox jumps over the lazy dog.",
      "When it rains, I stay indoors.",
      "She is studying to become a doctor."
    ]
  },
  "options": {
    "export_format": {
      "type": "string",
      "enum": ["json", "csv"],
      "default": "json",
      "description": "Export format for analysis results"
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "input": {
        "type": "string",
        "description": "Original input sentence"
      },
      "linguistic": {
        "type": "object",
        "description": "spaCy linguistic analysis (tokens, POS, dependencies, noun chunks, tense)"
      },
      "pedagogical": {
        "type": "object",
        "description": "LLM-generated pedagogical structure (sentence type, clauses, relations)"
      },
      "warnings": {
        "type": "object",
        "description": "Grammar warnings from LanguageTool or heuristic checks"
      },
      "export": {
        "type": "object",
        "description": "Exported analysis in JSON or CSV format"
      }
    }
  }
}
